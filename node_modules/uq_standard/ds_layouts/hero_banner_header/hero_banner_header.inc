<?php

/**
 * @file
 * Panel configuration options for editors.
 */

/**
 * Add custom Display Suite layout.
 *
 * @return array
 *   Layout configurations.
 */
function ds_hero_banner_header() {
  return array(
    'label' => t('Component: Hero banner header'),
    'regions' => array(
      'ds_image' => t('Image'),
      'ds_title' => t('Title'),
      'ds_content' => t('Content'),
    ),
    // Add this line if there is a default css file.
    'css' => FALSE,
    // Add this line if you're using DS 2.x for icon preview.
    'image' => TRUE,
  );
}
