<?php

/**
 * @file
 * Default theme implementation for a single paragraph item.
 *
 * Available variables:
 * - $content: An array of content items. Use render($content) to print them
 *   all, or print a subset such as render($content['field_example']). Use
 *   hide($content['field_example']) to temporarily suppress the printing of a
 *   given element.
 * - $classes: String of classes that can be used to style contextually through
 *   CSS. It can be manipulated through the variable $classes_array from
 *   preprocess functions. By default the following classes are available, where
 *   the parts enclosed by {} are replaced by the appropriate values:
 *   - entity
 *   - entity-paragraphs-item
 *   - paragraphs-item-{bundle}
 *
 * Other variables:
 * - $classes_array: Array of html class attribute values. It is flattened
 *   into a string within the variable $classes.
 *
 * @see template_preprocess()
 * @see template_preprocess_entity()
 * @see template_process()
 */
?>
<?php if (!empty($content)): ?>
  <div class="<?php print $classes; ?>"<?php print $attributes; ?>>
    <div class="content"<?php print $content_attributes; ?>>
      <?php
      if ($title_suffix):
        print render($title_suffix);
      endif;
      ?>

      <?php
      // Check if author type is set to profile reference or custom author then
      // render the correct one.
      if ($content['field_uq_blog_author_type']['#items'][0]['value'] == 'entity'):
        print render($content['field_uq_blog_author_profile_ref']);
      else: ?>
        <?php
        if (isset($content['field_uq_blog_author_photo'])):
          print render($content['field_uq_blog_author_photo']);
        endif;
        ?>
        <?php
        if (isset($content['field_uq_blog_author_name'])):
          print render($content['field_uq_blog_author_name']);
        endif;
        ?>
      <?php endif; ?>
    </div>
  </div>
<?php endif; ?>
